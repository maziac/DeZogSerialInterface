{
  "name": "dezogserialif",
  "version": "1.2.0",
  "description": "Program to allow DeZog communicating with a ZX Next through a serial interface.",
  "main": "./out/main.js",
  "scripts": {
    "prepublish": "tsc -p ./src",
    "compile": "tsc --project ./tsconfig.json -p ./src",
    "tslint": "tslint ./src/**/*.ts",
    "watch": "tsc --project ./tsconfig.json -w -p ./src",
    "package": "vsce package",
    "publish": "vsce publish"
  },
  "author": "Thomas Busse",
  "dependencies": {
    "bl": "^5.0.0",
    "serialport": "^10.3.0",
    "source-map-support": "^0.5.21",
    "tcp-port-used": "^1.0.2",
    "typescript": "^4.5.5"
  },
  "devDependencies": {
    "@types/node": "17.0.21",
    "ts-node": "^10.5.0"
  }
}
